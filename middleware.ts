
// This file is intentionally left empty to prevent any potential routing conflicts
// with the vercel.json configuration. The rewrite logic is fully handled by vercel.json.
export const config = {
  matcher: [],
};
export default function middleware() {}
